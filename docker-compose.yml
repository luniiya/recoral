name: recoral

services:
  recoral:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DATA_DIR=/data
    volumes:
      - recoral-data:/data
    restart: unless-stopped

volumes:
  recoral-data:
